let privateKey = ""; // Authorization private key
let contract = "";  // Fluence contract address
let appId = 0; // Deployed database id
let ethereumUrl = ""; // Ethereum light node URL


export {privateKey, contract, appId, ethereumUrl}
